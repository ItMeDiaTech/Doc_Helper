<Window x:Class="DocHelper.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DocHelper"
        mc:Ignorable="d"
        Title="Settings" Height="450" Width="600"
        WindowStartupLocation="CenterOwner">
    <Grid Background="White" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Tab Control -->
        <TabControl Grid.Row="0">
            <!-- API Settings Tab -->
            <TabItem Header="API Settings">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- API URL Label -->
                    <TextBlock Grid.Row="0" 
                               Text="API URL:" 
                               FontSize="14" 
                               FontWeight="Bold" 
                               Margin="0,0,0,5"/>
                    
                    <!-- API URL TextBox -->
                    <TextBox Grid.Row="1" 
                             Text="{Binding ApiUrl, UpdateSourceTrigger=PropertyChanged}" 
                             Padding="5" 
                             Margin="0,0,0,20"/>
                    
                    <!-- Spacer -->
                    <Grid Grid.Row="2"/>
                </Grid>
            </TabItem>
            
            <!-- Updates Tab -->
            <TabItem Header="Updates">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Auto Check for Updates Checkbox -->
                    <CheckBox Grid.Row="0" 
                              Content="Auto check for updates on startup" 
                              IsChecked="{Binding AutoCheckForUpdates}"
                              FontSize="14"
                              Margin="0,0,0,20"/>
                    
                    <!-- Current Version -->
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Current Version: " FontWeight="Bold" FontSize="14"/>
                        <TextBlock Text="{Binding CurrentVersion}" FontSize="14"/>
                    </StackPanel>
                    
                    <!-- Available Version -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Available Version: " FontWeight="Bold" FontSize="14"/>
                        <TextBlock Text="{Binding AvailableVersion}" FontSize="14"/>
                    </StackPanel>
                    
                    <!-- Update Status -->
                    <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,20">
                        <TextBlock Text="Status: " FontWeight="Bold" FontSize="14"/>
                        <TextBlock Text="{Binding UpdateStatus}" FontSize="14"/>
                    </StackPanel>
                    
                    <!-- Check for Updates Button -->
                    <Button Grid.Row="4" 
                            Content="Check for Updates" 
                            HorizontalAlignment="Left" 
                            Padding="20,10" 
                            Command="{Binding CheckForUpdatesCommand}"
                            IsEnabled="{Binding IsCheckingForUpdates, Converter={x:Static local:BooleanToInverseBooleanConverter.Instance}}"
                            Margin="0,0,0,20"/>
                    
                    <!-- Progress Indicator -->
                    <ProgressBar Grid.Row="5" 
                                 Height="10" 
                                 IsIndeterminate="{Binding IsCheckingForUpdates}"
                                 Visibility="{Binding IsCheckingForUpdates, Converter={x:Static local:BooleanToVisibilityConverter.Instance}}"
                                 Margin="0,0,0,20"/>
                    
                    <!-- Spacer -->
                    <Grid Grid.Row="7"/>
                </Grid>
            </TabItem>
        </TabControl>
        
        <!-- Save Settings Button -->
        <Button Grid.Row="1" 
                Content="Save Settings" 
                HorizontalAlignment="Right" 
                Padding="20,10" 
                Command="{Binding SaveSettingsCommand}"
                Margin="0,20,0,0"/>
    </Grid>
</Window>